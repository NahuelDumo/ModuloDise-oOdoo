<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Vista heredada de contactos para agregar campo Usuario Diseño -->
        <record id="view_partner_form_design_user" model="ir.ui.view">
            <field name="name">res.partner.form.design.user</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='category_id']" position="after">
                    <field name="is_design_user" widget="boolean_toggle"/>
                </xpath>
            </field>
        </record>

        <!-- Vista de lista para mostrar el campo -->
        <record id="view_partner_tree_design_user" model="ir.ui.view">
            <field name="name">res.partner.tree.design.user</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_tree"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='email']" position="after">
                    <field name="is_design_user" widget="boolean_toggle"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
